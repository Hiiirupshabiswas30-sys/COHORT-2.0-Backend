==> Authentication
 => register(1. user ka data save krna. 2. token user ko dena)
 => login
 => logout(token blacklisted)
 => Otp based registration

==> POST
    => create{ caption:string, img_url,user,createdAt}
    => can see the feed
    => like posts (cpllection types)
    => save post

==> Users
    => followers
    => following

  //  user = {
        username:string,
        email:string,
        passwoord:string,
        bio:string,
        followers:array,
        profile_image:string
    }